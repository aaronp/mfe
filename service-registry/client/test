#!/usr/bin/env -S scala-cli shebang -S 3 --dependency "com.lihaoyi::requests:0.8.0"

val url = args.headOption.getOrElse(sys.env.get("URL"))
println (s"url is $url from args " + args.mkString(","))
sys.env.foreach(println)